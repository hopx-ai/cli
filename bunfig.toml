# Bun configuration for Hopx CLI

[install]
# Use exact versions for reproducibility
exact = true

[run]
# Enable source maps for better error traces
sourcemap = "inline"

[test]
# Test configuration
coverage = true
coverageDir = "./coverage"

# Compile configuration for standalone binaries
# Use: bun build --compile ./src/index.ts --outfile hopx
